pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: "*.{ts,tsx,js,jsx,json,css,html,md}"
      run: bunx oxfmt --check {staged_files}

    - name: lint
      glob: "*.{ts,tsx}"
      run: bunx oxlint {staged_files}
